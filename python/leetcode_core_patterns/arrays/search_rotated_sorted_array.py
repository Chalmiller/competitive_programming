class Solution:
    def search(self, nums: List[int], target: int) -> int:
        """
        Task: Find the pivot point in the rotated sorted array
        Intuition: This is a modified binary search problem

        Algorithm:
          - Agnostic binary search 
          - brute force is easier
        """