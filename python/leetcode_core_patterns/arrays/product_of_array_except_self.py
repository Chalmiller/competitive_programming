class Solution:
  def productExceptSelf(self, nums: List[int]) -> List[int]:
    """
    Task: Return an array that has its elements as the product of all arrays except itself

    Algorithm:
      1. 
    """
    # product of the array to the left
    left_prod = []
    for i in range(len(nums)-1, -1, -1):
      left_prod.append()
